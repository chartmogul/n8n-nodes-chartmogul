# Setup

Prepare your local n8n for testing

```
  mkdir ~/.n8n/custom
	cd ~/.n8n/custom
	npm init -y
```

In the n8n node folder, run

```
	npm run rebuild && npm link
```

In the n8n installation folder, run

```
	npm link n8n-nodes-chartmogul
```
